#!/usr/bin/env python3
"""
this module provides a function to  turn a float into a string
"""


def to_str(n: float) -> str:
    """
    Converts a float into a string.

    Args:
        n (float): the number
    Returns:
        str: The input number converted to a string.
    """
    return str(n)
